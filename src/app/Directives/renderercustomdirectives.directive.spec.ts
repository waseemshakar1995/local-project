import { RenderercustomdirectivesDirective } from './renderercustomdirectives.directive';

describe('RenderercustomdirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderercustomdirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
